# A proof of concept for analyzing projects with source generators

## Building and runing the sample

`dotnet build --no-incremental`

`dotnet run --project SourceGeneratorPOC\SourceGeneratorPOC.csproj`
